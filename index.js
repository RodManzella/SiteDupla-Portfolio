// window.addEventListener("scroll", () => {
//     const image = document.getElementById("base.png");
//     const scrollY = window.scrollY;
//     const translateY = -scrollY / 2; // Ajuste o valor para controlar a velocidade da subida

//     image.style.transform = `translateY(${translateY}px)`;
// });